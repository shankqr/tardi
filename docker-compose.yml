services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tardi
      POSTGRES_USER: tardi
      POSTGRES_PASSWORD: tardi
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
